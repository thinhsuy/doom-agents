node_modules/
dist/
.vite/
*.local
*.tsbuildinfo

# Generated by build.py — exported from Postgres. Regenerate with `npm run data`.
src/data/agents.json
src/data/decisions.json
src/data/workspace.json
src/data/monitor.json

# The repo root ignores lockfiles globally (the catalog has no npm project).
# This app is a real npm project, so its lockfile IS committed — reproducible installs.
!package-lock.json
